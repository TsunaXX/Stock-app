name: Streamlit Keepalive

on:
  schedule:
    # Community Cloud hibernates apps after 12 hours without traffic.
    # Visit the app every 4 hours to leave a wide safety margin.
    - cron: "17 */4 * * *"
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/streamlit-keepalive.yml"

permissions:
  contents: read

concurrency:
  group: streamlit-keepalive
  cancel-in-progress: true

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Install Selenium
        run: python -m pip install --disable-pip-version-check --quiet "selenium>=4.26,<5"

      - name: Visit Streamlit app with a real browser session
        env:
          STREAMLIT_APP_URL: https://stock-app-qjzbqsoqs4hylyypaddep4.streamlit.app/
          STREAMLIT_HEALTH_URL: https://stock-app-qjzbqsoqs4hylyypaddep4.streamlit.app/_stcore/health
        run: |
          python - <<'PY'
          import os
          import shutil
          import time
          from urllib.parse import urlparse

          from selenium import webdriver
          from selenium.webdriver.common.by import By
          from selenium.webdriver.chrome.options import Options

          app_url = os.environ["STREAMLIT_APP_URL"]
          health_url = os.environ["STREAMLIT_HEALTH_URL"]
          expected_host = urlparse(app_url).hostname

          chrome = shutil.which("google-chrome") or shutil.which("google-chrome-stable")
          if not chrome:
              raise SystemExit("Google Chrome is not available on this GitHub runner.")

          options = Options()
          options.binary_location = chrome
          options.add_argument("--headless=new")
          options.add_argument("--no-sandbox")
          options.add_argument("--disable-dev-shm-usage")
          options.add_argument("--disable-gpu")
          options.add_argument("--window-size=1280,900")
          options.add_argument("--lang=en-US")

          driver = webdriver.Chrome(options=options)
          driver.set_page_load_timeout(60)

          try:
              # Community Cloud now establishes even public anonymous sessions through
              # JavaScript + cookies. A plain curl request can therefore be redirected
              # through /-/auth/app even when the app is public.
              driver.get(app_url)
              time.sleep(12)

              # Self-heal if a scheduled run was missed and the app has already slept.
              body_text = driver.find_element(By.TAG_NAME, "body").text.lower()
              if "gone to sleep" in body_text or "get this app back up" in body_text:
                  wake_button = None
                  for button in driver.find_elements(By.TAG_NAME, "button"):
                      if "get this app back up" in button.text.lower():
                          wake_button = button
                          break
                  if wake_button is None:
                      raise SystemExit("Streamlit app is sleeping but the wake button was not found.")
                  wake_button.click()
                  time.sleep(20)
                  driver.get(app_url)
                  time.sleep(10)

              current = urlparse(driver.current_url)
              if current.hostname != expected_host or current.path.startswith("/-/login"):
                  raise SystemExit(
                      f"Browser did not finish an anonymous public Streamlit session: {driver.current_url}"
                  )

              # Verify health using the SAME browser session/cookies established above.
              driver.get(health_url)
              time.sleep(5)
              health_text = driver.find_element(By.TAG_NAME, "body").text.strip().lower()
              health_current = urlparse(driver.current_url)

              if health_current.hostname != expected_host or health_text != "ok":
                  raise SystemExit(
                      "Streamlit browser keepalive failed health verification: "
                      f"url={driver.current_url!r}, body={health_text[:200]!r}"
                  )

              print("Streamlit keepalive succeeded: browser session established and health returned ok.")
          finally:
              driver.quit()
          PY
